1. Парсинг данных Api и скрапинг 
 • Сбор DataFrame;
 2. Очистка данных
 •Анализ и чистка пропусков и данных в целом 
 • Удаление лишних символов и дубликатов;
 • Переименование столбцов (Город (запрос) → Город, Категория (запрос) → Категория);
 • Удаление строк, где пропусков слишком много (более 50% по стране);
 • Исключение всех заведений со статусами CLOSED_*, так как для анализа нужны только действующие.
 4. Обработка пропусков и нормализация
 • Расчёт доли пропусков по городам и странам;
 • Удаление стран, где доля пропусков превышает порог;
 • Заполнение или удаление строк с пустой ценовой категорией;
 • Очистка текстовых значений от неразрывных пробелов и невидимых символов.
 5. Сегментация по цене
 • Создание функции segment(price), делящей заведения на группы:
 • дешево — нормированная цена ≤ 1
 • средне — 1 < цена < 2
 • премиум — цена > 2
 • Формирование трёх поддатасетов: эконом, средний, премиум.
 6. Анализ по странам
 • Расчёт средней нормированной цены по каждой стране;
 • Тепловые карты и диаграммы для визуализации ценовых уровней;
 • Корреляционный анализ между количеством заведений и средней ценой.
 7. Обогащение данными
 • Объединение с индексом Numbeo (Restaurant Price Index) по странам;
 • Добавление коэффициента Restaurant Price Index для нормирования цен.
 8. Итоговая сегментация стран
 • Создание сводной таблицы с долями сегментов по странам
• Добавление признака доминирующий_сегмент (где доля максимальна);
 • Назначение каждой стране категории в основном df
